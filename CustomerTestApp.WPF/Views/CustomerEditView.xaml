<UserControl x:Class="CustomerTestApp.WPF.Views.CustomerEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomerTestApp.WPF.Views" 
             xmlns:viewModels="clr-namespace:CustomerTestApp.WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Customer ID:"/>
            <TextBox Text="{Binding EditableCustomer.Id}" IsReadOnly="True"/>
            <TextBlock Text="First Name:"/>
            <TextBox Text="{Binding EditableCustomer.FirstName, Mode=TwoWay}"/>
            <TextBlock Text="Last Name:"/>
            <TextBox Text="{Binding EditableCustomer.LastName, Mode=TwoWay}"/>
            <TextBlock Text="Email:"/>
            <TextBox Text="{Binding EditableCustomer.Email, Mode=TwoWay}" />
            <TextBlock Text="Discount:"/>
            <TextBox Text="{Binding EditableCustomer.Discount, Mode=TwoWay}" />
            <Button Content="Save" Command="{Binding SaveCustomerCommand}" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
