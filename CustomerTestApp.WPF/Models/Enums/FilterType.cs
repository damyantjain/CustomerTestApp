namespace CustomerTestApp.WPF.Models
{
    /// <summary>
    /// The FilterType enum defines the filter types for customers.
    /// </summary>
    public enum FilterType
    {
        All,
        Name,
        Email
    }
}
